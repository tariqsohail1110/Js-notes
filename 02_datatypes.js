"use strict"; //Treating the whole code according to new JS standards

/*
ECMA => A JavaScript centralized standard (link => https://tc39.es/ecma262/)
MDN => Easy Documentations
*/
let name = "Tariq"
let age = 20
let isLoggedIn = true
let state;
/*
DATATYPES IN JS
number => ranges from 2 to 2^53 (-(2^53 for negative numbers))
bigint => for bigger values
string => ""
boolean => true / false
null => sandalone value (returns object datatype)
undefined => variable that hasn't defined
symbol => unique
*/

console.log(typeof undefined); //undefined
console.log(typeof null); //object (it can be a flaw in the language, while some say that it's the syntax)

